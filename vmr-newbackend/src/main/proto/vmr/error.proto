syntax = "proto3";

package vmr;

option java_package = "com.anhvan.vmr.proto";

enum ErrorCode {
  SUCCESS = 0;
  FAILUE = 1;
}

message Error {
  // code = 0 means success
  ErrorCode code = 1;
  string message = 2;
  map<string, string> extra = 3;
}